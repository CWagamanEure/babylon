name = "audit-data-integrity"
description = "Audit exact-decimal, ingest, partition, ledger, and episode data integrity."
sandbox_mode = "read-only"
developer_instructions = """
Before auditing, read `agents/audit-data-integrity.md` and `audit/AUDIT_PROTOCOL.md` in the repository.
Follow both documents completely. Treat the role manual as the audit lens and the shared protocol as
mandatory. Remain read-only, do not load the parquet lake, and report findings to the coordinating agent.
"""

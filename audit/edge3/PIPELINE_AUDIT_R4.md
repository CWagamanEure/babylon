# Pipeline audit round 4 (report/notebook/spec) — 2026-07-02

53-agent workflow: 21 confirmed, 3 killed.

## C1 [MAJOR] (figures) B9 fix incomplete: fig 04 was fixed but cmd_characterize — the source of the chat month-table and the spec's cross-check numbers — still prints a duplicate 'Mar' row whose BTC return is computed from TEST-window bars

**Mechanism:** report_figs.py fig 04 was fixed (line 88 loads bars only for the 6 TRAIN months, line 96 clamps ib to the last train bar, line 92 labels month 8 'Mar*'), but mlscreen_reports.py cmd_characterize is byte-identical to what round-3 B9 cited: line 103 buckets by 30-day epochs, lines 111-112 load bars from ALL_MONTHS 'if exists' — and bars_202604/202605 (test window) DO exist on disk — line 116 sets the m=8 BTC window to Mar 29–Apr 28, line 119 labels it MONTH_NAMES[min(8,7)]='Mar' (duplicate row), and lines 120-123 count the 3-day sliver as a full month in the profitable-month breadth denominator.
**Evidence:** /Users/corywagamaneure/bablyon/scratch_conv/mlscreen_reports.py:103,111-119,120-123 (identical to the lines cited in audit/edge3/PIPELINE_AUDIT_R3.md B9); /Users/corywagamaneure/bablyon/scratch_conv/mlscreen/ contains bars_202604.parquet and bars_202605.parquet so the 'if exists' filter admits them; contrast the fixed figure path /Users/corywagamaneure/bablyon/notebooks/report_figs.py:88,92,95-98; REPLICATION_SPEC.md:77-81 tells replicators to cross-check against these cmd_characterize outputs ('10/50 wallets profitable in every active month').
**Effect:** The chat table's March BTC context is ~90% April's (test-window) move, contradicting the script's own 'no test contact' docstring (mlscreen_reports.py:2-3), and the circulated '10/50 profitable in every active month' / 'months X/X' counts are judged against a 3-day pseudo-month (understating breadth, e.g. a wallet green Oct–Mar but red on Mar 29-31 drops out of the 10). Fig 04 and the chat/spec numbers now disagree on the same statistic, and the spec propagates the unfixed numbers as the replication target.

---

## C2 [MAJOR] (figures) REPLICATION_SPEC says monthly diagnostics use 'calendar months' but every implementation uses 30-day epochs anchored at 2025-08-01 — a faithful replicator cannot reproduce figs 04/08/11 or the spec's own cross-check numbers

**Mechanism:** REPLICATION_SPEC.md:62 specifies 'monthly PnL (calendar months) + profitable-month breadth', but the code computes month = (ts - T0) // (30*DAY) with T0 = 2025-08-01 (report_figs.py:16-17,87; mlscreen_reports.py:103). The buckets drift off calendar: 'Oct'=Sep 30–Oct 30, 'Nov'=Oct 30–Nov 29, 'Mar'=Feb 27–Mar 29, plus a Mar 29-31 remainder. Fig 04's 'BTC monthly %' line and the profitable-month breadth are all computed on these epoch buckets, and the fig-04 BTC windows (report_figs.py:95-96) align to the epoch edges, not month edges.
**Evidence:** /Users/corywagamaneure/bablyon/notebooks/REPLICATION_SPEC.md:62 ('calendar months') and :77-81 (expected outputs incl. '10/50 wallets profitable in every active month'); /Users/corywagamaneure/bablyon/notebooks/report_figs.py:87 (month=((ts-T0)//(30*DAY))), :92 (epoch buckets labeled with calendar-month names), :95-96 (BTC windows at epoch edges); /Users/corywagamaneure/bablyon/scratch_conv/mlscreen_reports.py:103.
**Effect:** A replicating team that implements the spec as written (true calendar months) gets different monthly PnL rows, a different BTC-vs-PnL pairing at every bucket edge (2-3 day shifts, e.g. 'Mar' PnL includes Feb 27-28 and excludes Mar 29-31), and a different 'profitable every active month' count — they will fail the spec's own cross-check without any bug on their side, or worse, silently 'fix' their correct implementation to match the epoch-bucket numbers. Notably mlscreen2.py cmd_analyze condition 3b WAS converted to calendar months (mlscreen2.py:425-426), so the pipeline itself is now internally split between the two conventions.

---

## C3 [MAJOR] (figures) report_figs.py hardcodes the preliminary 6-month window while the notebook auto-regenerates figures: after the in-flight Aug/Sep backfill lands, figs 08/11 silently fold Aug+Sep into 'Oct', fig 04 grows unlabeled '0'/'1' rows, and fig 09/12 keep a wrong 'of 6' denominator

**Mechanism:** Entry months are computed from T0 = 2025-08-01 (report_figs.py:17,87), so today's data (starts 202510) yields months 2..8 and the code 'works'. But months/labels are hardcoded to the truncated window: months list (line 21), mnames starting at 2 (lines 92,149), clip(2,7) (line 151), 'of 6 train months' axes (lines 176,248). The backfill of 202508/202509 is an open task (tasks #1-#3) and prereg v1.3 marks the current window PRELIMINARY; notebook cell 1 re-runs report_figs.py via subprocess on every execution. Once extract50 reruns over cand2_202508/202509, e contains months 0 and 1: fig 08/11's clip(2,7) relabels ALL Aug+Sep entries as 'Oct' (inflating the Oct bar and heatmap column), fig 04 plots bars labeled '0' and '1' (mnames.get fallback, line 97) with NaN BTC (bars list still Oct–Mar, line 88 → ib>ia fails), and fig 09/12's presence histograms still say 'months with trading activity (of 6 train months)' over an 8-month window while pres is built from only 6 stats files (lines 164-166).
**Evidence:** /Users/corywagamaneure/bablyon/notebooks/report_figs.py:21,88,92,97,149-151,164-166,176,248; informed_wallets_report.ipynb cell 1 (subprocess re-run of report_figs.py); task list items #1-#3 (backfill/extract pending); PREREG_WALLET_SCREEN.md:117-118 (current numbers marked PRELIMINARY pending the full window).
**Effect:** The final report — the version regenerated after tonight's look on the full window — silently mislabels two whole months of roster activity as 'Oct' in the persistence figures (the exact figures answering 'not one lucky month'), adds unexplained '0'/'1' bars to the monthly-PnL-vs-BTC figure, and understates months-active denominators, all without any error being raised.

---

## C4 [MINOR] (figures) Figs 08/11 DROP the Mar 29-31 sliver while the code comment claims it is folded into Mar — and the three deliverables now treat those same 3 days three different ways

**Mechanism:** report_figs.py:150: e2 = e.filter(pl.col('month') <= 7) with comment 'fold the 3-day sliver into Mar' — the filter EXCLUDES month-8 entries, and the subsequent .clip(2,7) (line 151) is a no-op for month 8 because those rows are already gone. So fig 08 (roster monthly activity) and fig 11 (per-wallet heatmap) omit all entries from Mar 29-31, whereas fig 04 shows them as a separate 'Mar*' bar (line 92) and cmd_characterize counts them as a standalone month in the 'months X/X' breadth (mlscreen_reports.py:120-123).
**Evidence:** /Users/corywagamaneure/bablyon/notebooks/report_figs.py:150-151 (filter-then-clip with contradicting comment), :92 (fig 04 'Mar*'), /Users/corywagamaneure/bablyon/scratch_conv/mlscreen_reports.py:120-123.
**Effect:** Fig 08's 'the roster shows up every month' bar and fig 11's March heatmap cells are computed on a truncated March (a wallet active or profitable only in Mar 29-31 shows as inactive/zero), and the report's three deliverables give three inconsistent answers for the same 3 days — separate 'Mar*' bucket (fig 04), silently deleted (figs 08/11), counted as a full month (chat table / spec numbers). Any reader reconciling the heatmap against the 'months X/X' table will find contradictions.

---

## C5 [MINOR] (figures) Presentation inconsistencies: fig 12 skill panel clips at ±250 without a clip note (unlike fig 03), and fig 11's fill_null(0) renders inactive wallet-months identically to $0 PnL while the chat table excludes inactive months

**Mechanism:** report_figs.py:250-254: a[5] histograms np.clip(wb, -250, 250) but the xlabel is 'mean 24h markout (bps/entry)' with no 'clipped' qualifier — fig 03 (line 80) does label its ±300 clip — so mass beyond ±250 piles into the edge bins and reads as real wallets at exactly ±250bp. report_figs.py:204-205: the heatmap pivot uses fill_null(0.0), so a wallet with zero entries in a month gets the exact RdYlGn midpoint color, indistinguishable from a wallet that traded and broke even; the chat table's 'months X/X' counts use only active months as the denominator (mlscreen_reports.py:121-123, tot=pl.len() over months present).
**Evidence:** /Users/corywagamaneure/bablyon/notebooks/report_figs.py:250-254 (unlabeled ±250 clip) vs :80 (fig 03 labels its clip); :204-205 (fill_null(0.0)); /Users/corywagamaneure/bablyon/scratch_conv/mlscreen_reports.py:121-123 (active-months-only denominator).
**Effect:** In fig 12 the field's extreme-loser/winner tails appear as spurious spikes at the ±250 edges of the skill panel; in fig 11 a reader cannot distinguish 'did not trade' from 'traded flat', so the heatmap visually overstates month coverage relative to the 'months X/X' persistence table built on active months only — two deliverables that will be read side-by-side disagree on what a neutral cell means.

---

## C6 [CRITICAL] (notebook-claims) Every characterization number in the notebook (LONG +74/SHORT +92, monthly PnL, 87% majors, ~36% crowded, 10/50, perma-long/short) is computed from cmd_extract50, which still has the un-fixed B4 reversal logic that mlscreen2 fixed

**Mechanism:** Round-3 B4 was fixed in mlscreen2.cmd_markout (opening COMPONENT: comp = |cum|-|pb| for same-sign, |cum| for flips, shrinking flips included — mlscreen2.py:320-327, prereg v1.3 item 4). But mlscreen_reports.cmd_extract50, which writes top50_entries.parquet — the sole data source for notebook figs 04-06/08/11 and the section 3/3b/4 numbers — still uses op = (np.abs(cum) > np.abs(pb)) & crossed with the FULL fill size so = sz[oi] (mlscreen_reports.py:82,86), booking closing units of sign-flip fills as entry markout/notional and silently dropping shrinking flips. B4 explicitly named this duplication; the fix was applied to the ranking path only.
**Evidence:** /Users/corywagamaneure/bablyon/scratch_conv/mlscreen_reports.py:82 ('op = (np.abs(cum) > np.abs(pb)) & crossed & (t + H24 < split_ms)') and :86 ('to, so = t[oi], sz[oi]') vs /Users/corywagamaneure/bablyon/scratch_conv/mlscreen2.py:316-327 (comp/opening component); PIPELINE_AUDIT_R3.md:32 ('identical logic is duplicated in mlscreen_reports.py cmd_extract50'); PREREG_WALLET_SCREEN.md:113-114 (v1.3.4); REPLICATION_SPEC.md:42-44 specifies the component convention while :4-5 names mlscreen_reports.py as the characterization reference.
**Effect:** The notebook's headline rebuttal numbers (cell 9 'long entries +74bp, short entries +92bp', cell 15 '87% majors / ~36% crowded', cell 12 monthly heatmap/breadth, the chat '10/50 every month' table) are measured under a different, prereg-violating entry definition than the roster ranking and tonight's test statistic — flip-heavy wallets' close-out PnL is misbooked as entry edge. A replicator following spec section 4 as written will NOT reproduce the spec's own 'Expected outputs' (+74/+92 etc.), and the report is internally inconsistent between its screen and its characterization.

---

## C7 [MAJOR] (notebook-claims) All monthly claims use 30-day blocks (mislabeled as calendar months) in violation of prereg v1.3.7, contradicting the replication spec's 'calendar months', and figs 08/11 silently drop Mar 29-31

**Mechanism:** Prereg v1.3 item 7 (B9/B15/B23) mandates calendar months for 'all monthly reporting'; the fix landed only in mlscreen2.cmd_analyze (calendar bounds, mlscreen2.py:426). report_figs.py:87 and mlscreen_reports.py:103 bucket by (ts-T0)//(30*DAY) from Aug 1, so the bar labeled 'Oct' is Sep 30-Oct 29, 'Mar' is Feb 27-Mar 28, and the overlaid 'BTC monthly %' uses the same shifted blocks. Additionally report_figs.py:150-151 does e.filter(month <= 7) BEFORE clip(2,7), so the month-8 sliver (Mar 29-31) is deleted, not 'folded into Mar' as the comment claims — figs 08 (activity) and 11 (heatmap) omit 3 days of March.
**Evidence:** /Users/corywagamaneure/bablyon/notebooks/report_figs.py:87 (month=((ts-T0)//(30*DAY))), :92 (mnames {2:'Oct'...8:'Mar*'}), :150-151 (filter then no-op clip vs '# fold the 3-day sliver into Mar'); /Users/corywagamaneure/bablyon/scratch_conv/mlscreen_reports.py:103,115-119; PREREG_WALLET_SCREEN.md:123-124; REPLICATION_SPEC.md:62 ('monthly PnL (calendar months)').
**Effect:** The claims 'profitable in every monthly bucket' (cell 9), 'all ~50, every month' and 'median wallet profitable in ~2/3 of its months' (cell 12), the heatmap, and the circulated '10/50 profitable every active month / months X/X' table are all computed on shifted pseudo-months the prereg forbids and the spec says are calendar months — replicators using calendar months will get different monthly-breadth numbers, and the monthly-consistency story can change at block boundaries (e.g. a crash week assigned to the wrong 'month').

---

## C8 [MAJOR] (notebook-claims) The verdict section (cell 19) demotes the co-primary follower-Sharpe cell with its locked PASS/FAIL rule to a mere 'companion', creating a selective-reporting escape hatch, and overstates 'locked before any test data was assembled'

**Mechanism:** Prereg v1.2 declares the markout family CO-PRIMARY 'alongside the follower-Sharpe cell', whose three-condition decision rule (Sharpe CI>0, >p95 of 1,000 placebos, sign-robustness; 'FAIL on any condition = the screen does not transfer; write the null') is at PREREG lines 41-50. Notebook cell 19 presents ONLY the markout statistic as the verdict and recasts the follower simulation as a 'Companion cell' answering copyability 'separately', with no decision rule stated. It also claims the design was 'locked before any test data was assembled', but the prereg itself records that Apr/May raw data pre-existed locally (line 14-15: backfill_202602-202605) and cmd_markout already wrote test_* aggregates into markout2.jsonl (mlscreen2.py:357-360) — only 'before any test-window READ' is defensible.
**Evidence:** informal_wallets_report.ipynb cell 19 ('Companion cell: follower-simulated PnL ... answered separately'; 'Design (locked before any test data was assembled)') vs /Users/corywagamaneure/bablyon/audit/edge3/PREREG_WALLET_SCREEN.md:41-50 (follower decision rule), :82-84 (co-primary), :14-15 (pre-existing test-month parquets); /Users/corywagamaneure/bablyon/scratch_conv/mlscreen2.py:357-360 (test fields written).
**Effect:** If tonight the markout cell passes and the follower cell fails, the notebook's framing lets the team fill in RESULT as a pass without reporting the co-primary FAIL — a forking-path liability the prereg was written to prevent. The 'never assembled' overstatement also weakens the report's integrity claim if challenged (test-window fills were extracted and aggregated, merely unread).

---

## C9 [MAJOR] (notebook-claims) Sections 3/3b affirmatively assert what prereg v1.3.8 (from B17) requires the report to disclaim, and the mandated per-side x per-month decomposition is absent

**Mechanism:** Prereg v1.3 item 8 obliges the report to present per-wallet medians, a per-side x per-month decomposition, and the explicit statement that plateau shape AND monthly-consistency stats match selection-conditioned noise. Cell 18 (section 5) carries the plateau and drift caveats but never extends them to monthly consistency or to the sided numbers; meanwhile cell 9 headlines 'Not a bull-market artifact ... Both sides work: long entries +74bp' and cell 12 asserts 'not one lucky month ... these are real traders, not a fitted grid' — the exact selection-conditioned claims B17 showed are unrebutted (the long leg is inflated by winner's curse; +74bp is raw, not drift-adjusted). No figure gives side x month or side-by-notional: fig 05 is a direction-mix scatter at 24h only (report_figs.py:106-115), the same 24h-only construction B17 criticized.
**Evidence:** informed_wallets_report.ipynb cells 9, 12, 18 vs /Users/corywagamaneure/bablyon/audit/edge3/PREREG_WALLET_SCREEN.md:125-129 (v1.3.8) and PIPELINE_AUDIT_R3.md:134-138 (B17: per-side eq-wt at 24h only, no drift adjustment, no side-by-notional; 'longs +XXbp is selection-inflated and undecomposed'); /Users/corywagamaneure/bablyon/notebooks/report_figs.py:106-115.
**Effect:** Readers will quote '+74bp long / +92bp short — both sides work' and 'profitable every month' as standalone evidence against the shorts-in-a-bear objection, which round 3 established they are not; the report is formally non-compliant with a prereg amendment it cites as passed, and the 'you just found shorts in a bear market' rebuttal remains missing from the artifact being circulated.

---

## C10 [MINOR] (notebook-claims) Two different 'eligible' counts circulate without reconciliation: the funnel ends at ~23.2k (activity band) while every downstream field/placebo is the ~22.5k >=30-entry population

**Mechanism:** Fig 01's funnel (report_figs.py:29-49) applies only the five activity-band steps, terminating at the pick count; the spec calls this '23,154 eligible of ~470k takers' (REPLICATION_SPEC.md:31). But the field in fig 02's legend ('{elig.sum():,} eligible wallets', report_figs.py:57,66), fig 03/12, cell 20's roster, and tonight's placebo pool all apply the additional >=30-train-entries-at-24h floor (spec:78 'field (22,455 eligible)'). The declared final eligibility step (prereg v1.3.2) is missing from the funnel figure, and the word 'eligible' flips meaning between spec section 2 and the expected-outputs block.
**Evidence:** /Users/corywagamaneure/bablyon/notebooks/report_figs.py:29-49 (funnel steps, no entry floor) vs :57 (elig = N[:,i24] >= 30) and :66 (legend 'eligible wallets'); /Users/corywagamaneure/bablyon/notebooks/REPLICATION_SPEC.md:31 vs :78; PREREG_WALLET_SCREEN.md:107-109 (floor declared but not in funnel).
**Effect:** A replicator cross-checking section 2's yield (23,154) against the term-structure figure's field label (22,455) sees a ~700-wallet discrepancy with no explanation in either artifact; more importantly the report's own funnel never reaches the population every subsequent statistic (and the placebo null) is drawn from, inviting the objection that a post-hoc filter sits between the advertised funnel and the analysis.

---

## C11 [MAJOR] (replication-spec) Characterization numbers (LONG +74/SHORT +92, 12/7 perma, 87% majors, ~36% crowded, 10/50) are computed under the OLD pre-fix entry convention — full fill size, shrinking flips dropped — not the spec §4 opening-component rule (audit B4 fix applied only to cmd_markout, not mlscreen_reports)

**Mechanism:** Spec §4 (:42-44) says the scored size is the position-increasing COMPONENT (sign-flip opens |new position|, never the full fill), which cmd_markout now implements (mlscreen2.py:320-327: comp=|cum|-|pb| same-sign, |cum| on flip, comp>1e-12). But top50_entries.parquet — the sole input to cmd_characterize and to figures 04/05/06/08/11 — is built by cmd_extract50 with the unfixed convention: op=(|cum|>|pb|)&crossed (mlscreen_reports.py:82) and so=sz[oi], the FULL signed fill size (:86), so sign-flip entries import the closing leg's size/PnL and magnitude-shrinking flips are excluded entirely. R3 finding B4 explicitly named this duplicate in mlscreen_reports.py:81-96; the fix landed only in mlscreen2.cmd_markout.
**Evidence:** scratch_conv/mlscreen_reports.py:82 (op = (np.abs(cum) > np.abs(pb)) & crossed), :86 (to, so = t[oi], sz[oi]) vs scratch_conv/mlscreen2.py:320-327 (component logic) and REPLICATION_SPEC.md:42-44; audit/edge3/PIPELINE_AUDIT_R3.md B4 ('identical logic is duplicated in mlscreen_reports.py cmd_extract50') with round-3 header claiming all confirmed findings fixed.
**Effect:** Every characterization number in the report, the chat table's per-wallet months counts, and the spec's own Expected outputs block (+74/+92, 12 perma-long/7 perma-short, 87% majors, ~36% crowded, 10/50) describe a different entry population and sizing than the screen statistic and the spec convention; a replicator implementing §4 for the §6 diagnostics will get materially different values and conclude the replication failed — or worse, the report's 'both sides work' and concentration claims still carry the flip-misattribution bias B4 was supposed to remove.

---

## C12 [MAJOR] (replication-spec) Spec's Expected outputs label -15.7/-105.9 and +81.1/+186.1 as 'eq-wt markout', but they are entry-pooled means (sum bps / sum n across all wallets) — a different statistic from the per-wallet equal-weight mean the spec §5 prescribes and tonight's look actually uses

**Mechanism:** Spec :78-79 presents field 'eq-wt markout -0.3bp@1h, -15.7@24h, -105.9@168h' and top-50 '+3.1/+81.1/+186.1'. Those numbers come from cmd_term/report_figs computing B.sum(axis=0)/N.sum(axis=0) — pooled per-ENTRY means in which a 10,000-entry wallet outweighs a 30-entry wallet 300:1 (mlscreen_reports.py:41-43, report_figs.py:60-61). Spec §5 (:58-59) instructs 'use the EQUAL-WEIGHT per-wallet mean bps' and the pre-registered decision statistic is exactly that per-wallet mean of means (mlscreen2.py:464-468 wallet_mean_bps). R3 B8 flagged the pooled construction; the fix was applied to cmd_look_markout only — the report-side numbers were regenerated with the unfixed pooled formula and then written into the spec under the equal-weight name.
**Evidence:** notebooks/REPLICATION_SPEC.md:58-59 vs :78-79; scratch_conv/mlscreen_reports.py:41-43 and notebooks/report_figs.py:60-61 (B.sum/N.sum) vs scratch_conv/mlscreen2.py:464-468 ((te_bps/te_n).mean() per wallet); PIPELINE_AUDIT_R3.md B8 (fix absent from the report path).
**Effect:** A replicator computing the spec's own equal-weight statistic on train data will not reproduce -15.7/+81.1/+186.1 and will either declare the replication broken or silently switch to the pooled convention — and if the pooled convention leaks into their placebo comparison it reintroduces the exact size-confound (B13/B8) the wallet-mean primary was adopted to kill. The report's headline term-structure figure is also not the statistic the pending verdict will be stated in, so tonight's number and figure 02 are not comparable.

---

## C13 [MAJOR] (replication-spec) Spec §6 promises 'monthly PnL (calendar months)' but the code uses 30-day pseudo-months anchored at Aug-1 T0 (with a 3-day Mar sliver bucket and per-wallet active-month denominators), and cmd_characterize's last-bucket BTC return still reads test-window bars — the 10/50 cross-check number is not reproducible under the spec's definition

**Mechanism:** cmd_characterize buckets entries by month=(ts-T0)//(30*DAY_MS) (mlscreen_reports.py:103) — blocks like Sep30-Oct29 anchored at Aug 1, not calendar months — labels both m=7 and the 3-day m=8 sliver 'Mar' via MONTH_NAMES[min(m,7)] (:119), computes its paired BTC return over T0+8·30d..T0+9·30d = Mar 29-Apr 28 from bars loaded for ALL_MONTHS including test months 202604/202605 (:111-118, B9 fix applied only in report_figs.py:88 via the 6-month list), and derives 'profitable in every active month' as positives over months-the-wallet-traded including the sliver (:120-123). report_figs fig-08's comment 'fold the 3-day sliver into Mar' (:150) is false — filter(month<=7) drops those entries before the no-op clip. Spec §6 (:61-62) declares calendar months.
**Evidence:** notebooks/REPLICATION_SPEC.md:61-62 vs scratch_conv/mlscreen_reports.py:103,111-123 and notebooks/report_figs.py:87,92,150; PIPELINE_AUDIT_R3.md B9/B20 (30-day-bucket and sliver findings, claimed fixed, still present in the reports path); mlscreen2.py:426 shows calendar bounds were implemented only in cmd_analyze.
**Effect:** The '10/50 wallets profitable in every active month' expected output, the monthly-PnL-vs-BTC figure, the heatmap, and the chat table's 'months X/X' counts are all defined on drifted 30-day buckets a spec-following replicator will not reproduce (calendar months shift ~1-2 days per bucket, remove the sliver month, and change the per-wallet breadth counts); the m=8 BTC context number still contains test-window prices, contradicting both the notebook's 'nothing reads test-window data' claim and the reports module docstring.

---

## C14 [MINOR] (replication-spec) Two under-pinned micro-conventions the spec leaves ambiguous will silently fork a replication: the $1 dust rule's price is unstated (code uses the current row's own FILL price) and 'exit lands before the split' is actually t+H < split, letting the exit pricing bar close up to ~35 minutes after the split

**Mechanism:** (a) Spec :37-38 says drop through 'the first time |position|*px < $1' without defining px; the code evaluates flatness with the fill row's own trade price (flat = np.abs(cum)*px < 1.0, mlscreen2.py:308, same in mlscreen_reports.py:74) — a replicator using bar close or mark price gets a different first-flat index ff, shifting the entire surviving fill stream, the entry/opening classification, and which coins are skipped. (b) Spec :52 says 'Train cell: exit lands before the split', but the code tests the horizon instant, tr = to+hms < day_split_ms (mlscreen2.py:343); the actual exit price is the close of the first bar after t+H, whose start may lag up to 30min (:156-160), so a train-ranked entry with t+H at Mar 31 23:55 is priced off a bar closing ~Apr 1 00:30 — a replicator excluding entries whose exit BAR crosses the split drops a boundary set the reference ranking includes.
**Evidence:** notebooks/REPLICATION_SPEC.md:37-38,52 vs scratch_conv/mlscreen2.py:308,343 and :156-160 (30-min next-bar-start guard); mlscreen_reports.py:74,82 (same dust rule, plus the t+H24<split train filter).
**Effect:** Both ambiguities produce silent, deterministic divergence between the two teams' entry sets — different leading-episode cuts change every downstream number per wallet-coin, and the split-boundary choice perturbs marginal top-50 membership — while a few minutes of post-split prices leak into the train ranking statistic. Small in magnitude, but exactly the class of unpinned convention the spec exists to pin; each needs one sentence in the spec (fill-row px; t+H-before-split, exit bar may overhang).

---

## C15 [CRITICAL] (fix-verification) Completeness gate checks input parquets at look time, not the provenance of the jsonl it consumes — the stale PARTIAL markout2.jsonl on disk will pass the gate tonight once backfill lands

**Mechanism:** _require_complete (mlscreen2.py:85-91) only asserts that bars/stats/cand2 parquet FILES exist for ALL_MONTHS at the moment cmd_look_markout (:448) or cmd_analyze (:393) runs. cmd_markout (:283-364) and cmd_daily (:164-277) have no gate and write no provenance into markout2.jsonl/daily2.jsonl (record schema :352-360 has no month-coverage or wallets2 pairing). So: build jsonl on partial data (warn-and-proceed _load_bars :101-105), let the 202508/202509/202606 backfill land, and the look's gate passes while reading the stale partial jsonl. Worse, _write_manifest (:94-98) records the months present AT LOOK TIME, so the manifest would falsely certify the partial-data look as complete. The manifest also never records the wallets2.txt version paired with cand2, so the B22 stale-pairing hole (re-extract only new months against a new pick) remains code-undetectable.
**Evidence:** mlscreen2.py:85-91 (existence-only gate), :292 and :175 (jsonl opened with no header/manifest), :94-98 (manifest reflects look-time state); disk state proves the hazard is live: scratch_conv/mlscreen/ has bars/stats/cand2 ONLY for 202510-202605, yet markout2.jsonl (Jul 2 11:44) and top50_entries.parquet (11:45) already exist from this partial set, and wallets2.txt (10:31) predates the cand2 batch (11:32-11:38).
**Effect:** Tonight's pre-registered look can silently spend itself on a 6-month-train / no-June-test markout2.jsonl while printing PASS/FAIL and a manifest that claims all 11 months were present — the exact B1/B16/B21 failure the v1.3 item-5 fix was supposed to close. The gate must bind to the jsonl's own build manifest (or force rebuild: refuse if markout2.jsonl is older than any cand2/bars file or than wallets2.txt).

---

## C16 [MAJOR] (fix-verification) Prereg v1.3 item 7 (calendar months for ALL monthly reporting) was never applied to mlscreen_reports.py — the monthly table, breadth stat, and the '10/50 profitable every active month' claim still use 30-day blocks with a mislabeled 3-day sliver and a test-window BTC return

**Mechanism:** cmd_characterize still buckets month=(ts-T0)//(30*DAY_MS) (mlscreen_reports.py:103), still clamps the label with MONTH_NAMES[min(m,7)] so the 3-day Mar-29..31 sliver prints as a second 'Mar' (:119), still computes that row's BTC return over T0+8*30d..T0+9*30d = Mar 29-Apr 28 using bars loaded from ALL_MONTHS including test months 202604-06 (:111-118), and still counts the sliver as a full month in the profitable-month-breadth denominator (:120-123). Only cmd_analyze's condition 3b got the calendar fix (mlscreen2.py:426); the reporting side the amendment explicitly covers ('all monthly reporting use calendar months') did not.
**Evidence:** mlscreen_reports.py:103, :115-119, :111-113, :120-123 at current HEAD vs PREREG_WALLET_SCREEN.md v1.3 item 7 (:123-124) and vs REPLICATION_SPEC.md:62-63 which promises 'monthly PnL (calendar months)'; REPLICATION_SPEC.md:81 and notebook cells 9/12-14 cite '10/50 wallets profitable in every active month' and monthly-heatmap figures derived from this bucketing.
**Effect:** The report's regime narrative and the chat table of 10 wallets with 'months X/X' counts are computed on misaligned pseudo-months (B9/B20 uncorrected): the duplicated 'Mar' row carries April's (test-window) BTC move, the breadth counts judge wallets against a noise-dominated 3-day bucket, and the module's 'no test contact' docstring is violated. The spec says calendar months, the code does 30-day blocks — a replicating team will silently produce different numbers.

---

## C17 [MAJOR] (fix-verification) Prereg v1.3 item 4 (reversal opening-component) was never applied to mlscreen_reports.py cmd_extract50 — top50_entries.parquet, regenerated AFTER the round-3 fixes, still books full flip size as entry and drops shrinking flips

**Mechanism:** cmd_extract50 still uses the pre-fix construction: op = (np.abs(cum) > np.abs(pb)) & crossed (mlscreen_reports.py:82) with the FULL fill size so = sz[oi] (:86) feeding notional=|so*ein| and m24/m72=so*(e-ein) (:94-96). mlscreen2.py's cmd_markout was fixed (comp/sign logic :320-327) but the duplicated logic B4 explicitly flagged in mlscreen_reports.py:81-96 was not, so the entry population and per-entry PnL in top50_entries.parquet differ systematically from the fixed ranking file for every wallet that reverses positions.
**Evidence:** mlscreen_reports.py:81-96 at HEAD (unchanged from the construction quoted verbatim in PIPELINE_AUDIT_R3.md B4) vs mlscreen2.py:316-327 (fixed); top50_entries.parquet mtime Jul 2 11:45 — rebuilt after the fixes with the old logic; PREREG_WALLET_SCREEN.md v1.3 item 4.
**Effect:** Every characterization number shown to the user and headed for the report — LONG +74 / SHORT +92 bp, 12 perma-long / 7 perma-short, 87% majors, ~36% crowded-day PnL share, the monthly table — is computed on an entry set that imports close-out PnL into 'entry markout' for flip-heavy wallets and omits shrinking-flip entries, and that no longer even matches the entry definition of the markout2.jsonl the roster was ranked on.

---

## C18 [MINOR] (fix-verification) Opening-component threshold comp > 1e-12 is absolute while cumsum float dust scales with position size — full-close fills in large-unit coins spawn dust-sized 'reversal entries' that carry full weight in the new eq-wt PRIMARY statistic

**Mechanism:** In mlscreen2.py:320-323, a taker fill that closes a position exactly leaves cum = float dust (cumsum rounding, magnitude ~eps * |position|); when the dust's sign is opposite pos_before, same=False so comp=|cum|=dust, and dust > 1e-12 holds for coins with unit counts >= ~1e4 (single-op error ~2e-10 at position 1e6 units). The entry is then scored with so = dust*sgn, but bps = sign(so)*(eout/ein-1)*1e4 (:339) has FULL magnitude and arbitrary sign, and n increments by 1 (:348). Since v1.3 item 1 made bps_sum/n averaged per wallet the primary look statistic (:464-468) and the eligibility floor is train_n >= 30 (:451), dust entries dilute per-wallet mean bps with zero-mean noise and inflate entry counts; dollar PnL and notional are unaffected (dust-sized).
**Evidence:** mlscreen2.py:320-323 (comp > 1e-12 absolute), :339 (bps independent of size), :348 (count), :451 (train_n floor), :464-468 (per-wallet mean bps primary statistic); contrast the flat detector which correctly uses a dollar threshold |cum|*px < 1.0 (:308).
**Effect:** For round-trip-heavy roster wallets in high-unit-count alts, the per-wallet mean test bps is diluted toward zero (roster and placebo both, so direction is power-loss/noise rather than false-pass), reported n counts are inflated, and marginal wallets can clear the >=30-entry eligibility floor on dust alone. A notional-based epsilon (e.g. comp*px > $1) would remove it; effect size is coin-mix dependent but structurally present in the decision statistic.

---

## C19 [MAJOR] (chat-deliverables) (b) B4 fix not propagated to cmd_extract50: every characterization number delivered in chat (and figs 04-06/08/11, spec §7 expected outputs) is computed under the OLD opening rule and full-fill sizing, inconsistent with the markout2.jsonl ranking and with tonight's look

**Mechanism:** mlscreen2.py cmd_markout at HEAD implements prereg v1.3.4: opening = position-increasing COMPONENT, comp = where(same, |cum|-|pb|, |cum|), so = comp*sgn_c, includes shrinking sign-flips (mlscreen2.py:316-327). But mlscreen_reports.py cmd_extract50, which builds top50_entries.parquet — the sole source of the direction split (+74/+92), perma-long/short counts (12/7), 87% majors, ~36% crowded-day PnL, the monthly table, and the 10/50 'all months profitable' stat — still uses the pre-fix rule: op = (|cum| > |pb|) & crossed (mlscreen_reports.py:82) and scores the FULL fill size so = sz[oi] (line 86). Reversal fills book the closing units' post-fill move into 'entry markout' with the new direction's sign (inflating turning-point wallets), and shrinking flips are dropped entirely. REPLICATION_SPEC.md:42-44 documents only the component rule, so a replicator following §4 then computing §6 characterization cannot reproduce the §7 expected outputs (lines 77-81).
**Evidence:** /Users/corywagamaneure/bablyon/scratch_conv/mlscreen_reports.py:82 ('op = (np.abs(cum) > np.abs(pb)) & crossed & (t + H24 < split_ms)') and :86 ('to, so = t[oi], sz[oi]') vs /Users/corywagamaneure/bablyon/scratch_conv/mlscreen2.py:320-327 (component rule per audit B4); PREREG_WALLET_SCREEN.md:113-114 (v1.3.4 declares the component convention); REPLICATION_SPEC.md:5 names mlscreen_reports.py as reference implementation for characterization. Round-3 audit B4 explicitly flagged the duplication in mlscreen_reports.py:81-96; the fix landed only in mlscreen2.py.
**Effect:** The chat tables' per-wallet PnL, direction, monthly, coin-share and crowding numbers describe a different entry population and different sizes than the train_pnl that selected the roster and than the entries the pre-registered look will score — the report's headline characterization is internally inconsistent with its own ranking file, and the spec propagates unreproducible 'expected outputs' silently. Must rerun extract50 with the component rule (per v1.3.5 rebuild order) before circulating.

---

## C20 [MAJOR] (chat-deliverables) (a) 'Profitable in every active month' table is statistically tautological for its low-month-count rows and uses 30-day T0-offset buckets while the spec claims (and prereg v1.3.7 mandates) calendar months

**Mechanism:** cmd_characterize counts months as (ts-T0)//30d (mlscreen_reports.py:103) with the per-wallet denominator = buckets in which the wallet traded (:120-123); the day-240+ sliver (~3 days) counts as a full month and MONTH_NAMES[min(m,7)] (:119) labels it 'Mar'. The chat table sorts by PnL, so it is dominated by wallets with few active buckets (5 of 10 have <=2), including a 1/1 wallet ($808k in one bucket). But any top-50 wallet was selected on large positive TOTAL train PnL; for a wallet active in one bucket that bucket IS the total, so 'profitable in its only month' holds with probability ~1 by construction — 'months 1/1' and largely '2/2' convey zero persistence information. Meanwhile REPLICATION_SPEC.md:62 says 'monthly PnL (calendar months)' and PREREG_WALLET_SCREEN.md:123-124 (v1.3.7) requires calendar months for ALL monthly reporting — the declared round-3 fix was never applied to mlscreen_reports.py. report_figs.py adds a third variant: the comment says 'fold the 3-day sliver into Mar' but e2 = e.filter(month <= 7) DROPS month-8 entries before the no-op clip(2,7) (report_figs.py:150-151), so figs 08/11 exclude Mar 29-31 while the 10/50 stat includes the sliver as a full month.
**Evidence:** /Users/corywagamaneure/bablyon/scratch_conv/mlscreen_reports.py:103,115-123; /Users/corywagamaneure/bablyon/notebooks/report_figs.py:87,150-151; /Users/corywagamaneure/bablyon/notebooks/REPLICATION_SPEC.md:62,81; /Users/corywagamaneure/bablyon/audit/edge3/PREREG_WALLET_SCREEN.md:123-124; chat context: 10/50 all-months, '$808k in ONE month' 1/1 row, 5 of 10 listed wallets <=2 active months.
**Effect:** The persistence headline ('10/50 profitable in every active month' + the named-wallet table) overstates evidence: the strongest-looking rows are selection tautologies, the 'month' unit contradicts the spec's own definition (silent replication failure of spec line 81), and the same report shows two different month partitions (heatmap drops the sliver, breadth stat counts it). Present X/X only for wallets with >=4-5 active calendar months, or drop the table.

---

## C21 [MINOR] (chat-deliverables) (c) Chat claim that 0x4b3cd947... 'is also in the babylon droplet's live roster' is unverifiable in the repo, and the closest local evidence points the opposite way (long-hold study scored it strongly negative)

**Mechanism:** No /Users/corywagamaneure/bablyon/data/follow/markout_live exists (path absent). The wallet appears locally only in (1) data/follow/markout_candidates.csv:705 — a 2,431-row alphabetical candidate list (header 'w', no scores), where membership means 'candidate', not 'live roster'; and (2) data/follow/long_hold_wallets.csv:1400 with longhold_skill_bps=-397.03, rank 1399, top_quintile=False — the prior wallet-follow study ranked it near the bottom. No registry/roster artifact readable in the repo substantiates 'live roster' membership.
**Evidence:** ls data/follow/: markout_live does not exist; grep hits: /Users/corywagamaneure/bablyon/data/follow/markout_candidates.csv:705 (bare address in 2,431-row list), /Users/corywagamaneure/bablyon/data/follow/long_hold_wallets.csv:1400 ('1399,0x4b3cd947...,-397.03,...,False'); wallet is rank 16 in notebooks/report_figs/top50_wallets.csv ($1.82M train PnL).
**Effect:** If the report repeats the 'independently corroborated by the live system' framing, it is an unsubstantiated claim; the only checkable prior evidence (long-hold skill -397bp, bottom-ranked) mildly contradicts it. Either cite the actual droplet registry artifact (with date and selection rule — candidate list membership is circular if it came from the same markout screen) or drop the corroboration sentence.

---


## Killed

- Spec's train window (Oct 1) and expected outputs are unreproducible from the reference implementation, whose constants hard-code Aug 1 and whose completeness gate rejects the very dataset that produced the numbers
- (e) Top-wallet 42,350 train entries arithmetically exceeds the eligibility band's own order caps (<=20,000 total, <=15/day ≈ 2,700 over the ~181-day window) with no reconciliation; the gap is driven by zhash (TWAP/liquidation) fills excluded from order COUNTING but included in entry SCORING
- (d) Report publishes 50 full wallet addresses (CSV + notebook table) labeled as 'informed flow' with dollar PnL, with no operational-security or ethics note
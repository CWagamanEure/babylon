# Audit findings

Each auditor writes `NN_<slug>.md` here (format in `../00_GROUND_RULES.md` §4). The synthesis
pass reads all of them into `../SUMMARY.md`, ranked by blast radius (gate-false-GO first).
Empty until the audit runs.
